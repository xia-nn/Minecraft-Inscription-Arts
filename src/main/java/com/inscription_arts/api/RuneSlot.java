package com.inscription_arts.api;

/**
 * 符文槽位。每件装备拥有三个固定槽位，对应铭刻的不同阶段效果。
 * <p>
 * 借鉴匠魂「部件拆分」思路：前缀 / 核心 / 后缀三段各司其职，玩家可自由组合。
 */
public enum RuneSlot {
    /** 前缀：决定装备的基础属性倾向（如耐久、速度） */
    PREFIX,
    /** 核心：决定装备的核心能力（如时运、伤害） */
    CORE,
    /** 后缀：提供额外特性，部分后缀可增加槽位 */
    SUFFIX
}
